#include <bits/stdc++.h>
using namespace std;

int main()
{
    string str = "Hello, world";
    // cout << str + " Hi" << endl; // Concatenation
    // cout << str.length() << endl;
    // cout << str.find("ello") << endl;
    // cout << str.substr(1, 5) << endl;
    // we can use relational operators ==, != , <=, >= etc..;
     reverse(str.begin(), str.end());
    cout << str;
    cout << (int)pow(10,0);
    return 0;
}